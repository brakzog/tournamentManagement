name: tournament_management
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:

  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  easy_localization: ^3.0.0
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  firebase_database: ^11.1.6
  flutter_secure_storage: ^9.2.2
  the_apple_sign_in: ^1.1.1
  google_sign_in: ^6.2.2
  date_formatter: ^0.0.4
  provider: ^6.1.2
  sign_in_with_apple: ^6.1.4

dependency_overrides:
  intl: ^0.19.0 




dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2 # Pour simuler des objets dans vos tests unitaires
  build_runner: ^2.4.6 # Nécessaire pour générer des mocks

flutter:
  uses-material-design: true
  assets:
    - assets/lang/
